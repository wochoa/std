Vue.component('cata-prioridades',require('../../components/tramite/catalogo/CataPrioridades').default);
Vue.component('cata-recepcion',require('../../components/tramite/catalogo/CataRecepcion').default);

/**
 * Next, we will create a fresh Vue application instance and attach it to
 * the page. Then, you may begin adding components to this application
 * or customize the JavaScript scaffolding to fit your unique needs.
 */

const app = new Vue({
    el: '#app'
});